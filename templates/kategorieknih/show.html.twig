{% extends 'base.html.twig' %}

{% block title %}Kategorie knih{% endblock %}

{% block body %}
    <h1>Kategorie knih</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ kategorieknih.id }}</td>
            </tr>
            <tr>
                <th>Název</th>
                <td>{{ kategorieknih.name }}</td>
            </tr>
            <tr>
                <th>Popis</th>
                <td>{{ kategorieknih.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('kategorieknih_index') }}">Zpátky na seznam</a>

    <a href="{{ path('kategorieknih_edit', {'id': kategorieknih.id}) }}">Upravit</a>

    {{ include('kategorieknih/_delete_form.html.twig') }}
{% endblock %}